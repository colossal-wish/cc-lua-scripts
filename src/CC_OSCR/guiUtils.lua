local width, height = term.getSize()

local guiUtils = {}

function guiUtils.createGrid(x, y, w, h, rows, columns)

end

function guiUtils.drawGrid(grid) end
function guiUtils.fillGridCell(grid, cellX, cellY) end
