local windows = require("modules.GUI.windows")
local buttons = require("modules.GUI.buttons")

--local win = windows.new(6, 4, 30, 10, "Test")

-- local commandButton = buttons.new(
--   term.current(),
--   "Command",
--   8,
--   8,
--   nil,
--   nil,
--   colors.red,
--   1,
--   shell.run("OSCR/programs/command")
-- )

-- commandButton:draw()

-- while true do
--   local event = {os.pullEvent()}
--   commandButton:handleEvent(event)
-- end

--shell.run("OSCR/programs/command")
--shell.run("os/.programs")

